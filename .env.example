# ============================================
# UTEC Planificador AI Agent - Environment Configuration
# ============================================
# This file serves as a template for environment variables.
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env in the project root:
#    cp .env.example .env
# 
# 2. Fill in your actual values (especially OPENAI_KEY)
# 
# 3. NEVER commit .env to git (already in .gitignore)
#
# USAGE:
# - Docker development: docker-compose up -d
# - Docker production: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# - Local development (no Docker): source venv/bin/activate && uvicorn app.main:app --reload
# ============================================

# ============================================
# Docker Compose Configuration
# ============================================
COMPOSE_PROJECT_NAME=utec-planificador

# ============================================
# OpenAI Configuration
# ============================================
# CRITICAL: You MUST set your OpenAI API key here
# Get your API key from: https://platform.openai.com/api-keys
# 
# Security Note:
# - NEVER commit your actual API key to git
# - Use environment-specific secrets management in production
# - Consider using Azure Key Vault, AWS Secrets Manager, or similar
OPENAI_KEY=your-openai-api-key-here

# ============================================
# Application Configuration
# ============================================
# Port mapping for AI Agent service
AI_AGENT_PORT=8000

# ============================================
# Session Management Configuration
# ============================================
# Time in minutes before inactive sessions are cleaned up
SESSION_TIMEOUT_MINUTES=20

# Interval in seconds between session cleanup runs
CLEANUP_INTERVAL_SECONDS=300

# ============================================
# Logging Configuration
# ============================================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================
# Health Check Configuration
# ============================================
HEALTHCHECK_INTERVAL=30s
HEALTHCHECK_TIMEOUT=10s
HEALTHCHECK_RETRIES=3
HEALTHCHECK_START_PERIOD=40s

# ============================================
# Docker Resource Limits (Production)
# ============================================
# AI Agent resource limits
AI_AGENT_CPU_LIMIT=2
AI_AGENT_MEMORY_LIMIT=2G
AI_AGENT_CPU_RESERVATION=1
AI_AGENT_MEMORY_RESERVATION=1G

# ============================================
# Docker Restart Policy (Production)
# ============================================
RESTART_POLICY_CONDITION=on-failure
RESTART_POLICY_DELAY=5s
RESTART_POLICY_MAX_ATTEMPTS=3
RESTART_POLICY_WINDOW=120s

# ============================================
# Docker Logging (Production)
# ============================================
LOG_DRIVER=json-file
LOG_MAX_SIZE=10m
LOG_MAX_FILE=3

# ============================================
# Network Configuration
# ============================================
# When integrating with backend, both services should share a network
# See integration instructions in README.md
